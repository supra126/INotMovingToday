# ============ AI Analysis (Gemini) ============
# Get your API key from: https://aistudio.google.com/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini Thinking Budget (optional, default: 2048)
# Controls the thinking budget for Gemini model responses
GEMINI_THINKING_BUDGET=2048

# ============ Video Generation ============
# Provider: mock (default for testing), runway, veo (Google Veo 3.1)
VIDEO_PROVIDER=mock

# Runway API Key (required if VIDEO_PROVIDER=runway)
# Get your API key from: https://runwayml.com/
# RUNWAY_API_KEY=your_runway_api_key_here

# Google Veo 3.1 (required if VIDEO_PROVIDER=veo)
# Uses the same GEMINI_API_KEY above - ensure your API key has Veo access enabled
# Veo supports up to 8 seconds of video at 720p resolution
#
# Veo Model Selection:
# - Default: Fast mode (veo-3.1-fast-generate-preview) - faster, lower cost
# - Set VEO_USE_STANDARD=true for Standard mode (veo-3.1-generate-preview) - higher quality
# VEO_USE_STANDARD=false

# ============ Build Mode ============
# Set to "static" for static export build
# NEXT_PUBLIC_BUILD_MODE=static
